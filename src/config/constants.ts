/**
 * Defines core application constants.
 */

// Prefix for all API routes
export const API_PREFIX = '/api';

// Export other constants as needed (e.g., pagination limits, default values)
// export const DEFAULT_LIMIT = 20;